<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hesolutions.demoui">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.NoActionBar.Fullscreen">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LeftSector" />
        <activity android:name=".CentreSector" />
        <activity android:name=".RightSector" />
        <activity android:name=".FunctionalPage" />
        <activity android:name=".SuitePage" />
        <activity android:name=".DisplayCamera" />
        <activity android:name=".ReportPage" />
        <activity android:name=".WaterPage"></activity>
    </application>

</manifest>
